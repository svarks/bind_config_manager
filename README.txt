This file is for you to describe the bind_config_manager application. Typically
you would include information such as the information below:

Installation and Setup
======================

Install ``bind_config_manager`` using easy_install::

    easy_install bind_config_manager

Make a config file as follows::

    paster make-config bind_config_manager config.ini

Tweak the config file as appropriate and then setup the application::

    paster setup-app config.ini

Then you are ready to go.
